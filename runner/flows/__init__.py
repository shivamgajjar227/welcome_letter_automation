"""
Automation stage implementations will be placed here.

Each module should expose a `run(stage_context)` function that receives the
shared WebDriver, metadata, and a mutable stage result object.
"""

